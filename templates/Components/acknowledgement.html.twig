<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acknowledgement</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        {{ source('./styles/acknowledgement.scss') }}
    </style>
</head>
<body>
    <div class="modal-dialog" role="document">
        <div class="modal-content{{ success ==1 ? ' success' : ' failed' }}">
            <div class="modal-body">
                <p>{{ message }}</p>
            </div>
            <div class="modal-footer">
                <a href="{{ path('app_index') }}" class="btn btn-primary ack">Return</a>
            </div>
        </div>
    </div>
</body>
</html>