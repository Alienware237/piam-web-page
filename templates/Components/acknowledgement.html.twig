<div class="modal-dialog" role="document">
    <div class="modal-content{{ success ? ' success' : ' failed' }}">
        <div class="modal-body">
            <p>{{ message }}</p>
        </div>
        <div class="modal-footer">
            <a href="{{ path('app_index') }}" class="btn btn-primary ack">Return</a>
        </div>
    </div>
</div>